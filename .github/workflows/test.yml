name: Test Project Workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4.1.1     
      - name: Install dependencies
        run: npm ci
      - name: Run tests
        run: npm test
    
  deploy:
    needs: Test Project Workflow
    runs-on: ubuntu-latest
    steps:
      - name: Get Code
        uses: actions/checkout@v4.1.1     
      - name: Install dependencies
        run: npm ci
      - name: Build project
        run: npm run build
      - name: deploy
        run: echo 'Deploying'